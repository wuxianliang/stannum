# Copyright (C) 2026 PlanetScale
#
# See LICENSE in the repository root for license terms.

[package]
name = "tokenizer"
version.workspace = true
authors.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
emojis = "0.6"
# Word-level segmenter for the `jieba` tokenizer preset (embedded dictionary,
# deterministic for a pinned jieba-rs version).
jieba-rs = "=0.7.4"
thiserror.workspace = true
# tinyvec 1.13.0 fails to compile with unicode-normalization's alloc-only features.
tinyvec = "=1.12.0"
unicode-normalization = "0.1.25"
unicode-properties = "0.1.4"
unicode-segmentation = { git = "https://github.com/eeeebbbbrrrr/unicode-segmentation.git", branch = "unicode-words-optimization" }

[dev-dependencies]
proptest = "1"
